@require 'add-font'

font-license()

/* Regular font is moved to the generic stylesheet to appear faster. */
/* The counter is used only in the regular stylesheet too */

add-font('21Cent-Bold', 'strong, b')

add-font('21Cent-Italic', 'em, i, q, time, .Metadata, blockquote, code, kbd')

add-font('21Cent-Black', '.Page-Content h1, h2, h3, h4, h5, h6')
