.Feedback
  overflow: hidden

  margin-top: 2*$size.base

  font-size: $size.base
  line-height: 1.5*$size.base

  color: $color.misc

  box-shadow: 0 -1px 0 rgba($color.dark, 0.15), inset 0 1px 0 rgba(#FFF, 0.9)

  && > p
    text-indent: 0
